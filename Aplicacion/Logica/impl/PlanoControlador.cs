using Dominio;

namespace Logica
{
    internal class PlanoControlador : IPlano
    {

        public void EliminarPlano(string  nombre)
        {
            //Plano plano = ObtenerPlano(nombre);
            
              //  planoDB.EliminarPedido(pedido);
            
        }
    }
}